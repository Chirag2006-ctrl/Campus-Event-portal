PORT=3000
HOST=127.0.0.1
DB_PATH=./data/campus.db
# Generate with: node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
ADMIN_TOKEN=replace_with_a_random_secret_at_least_24_characters
