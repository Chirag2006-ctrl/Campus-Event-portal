{
  "name": "campus-event-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Campus event portal with a Node.js API and persistent SQLite registrations",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
